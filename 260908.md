# 260908 TIL

## 배운 점

<a href="table">배운 점 : 표 태그</a> / <a href="form">배운 점 : 폼 태그</a> / <a href="divide">배운 점 : div, span 태그</a>

<br>

<h2 id="#table">배운 점 : 표 태그</id></h2>

- 표를 만드는 table 태그에 대해 배웠다.

- table에 border(table border="n")를 넣어 테두리를 넣을 수 있다.
  - table에 rowspan(table rowspan="n")을 넣어 열을, colspan(table
    colspan="n")을 넣어 행을 합칠 수 있다.

- 표를 작성할 때 쓰는 3가지 코드로 thead, tbody, tfoot이 있다.

<br>

<h2 id="#form">배운 점 : 폼 태그</id></h2>

- form 태그는 입력창을 나타내는 태그이다.
  - 입력값들을 보낼 서버를 나타내는 action과, 보내는 방식을 나타내는 method 태그로 구성되어 있다.
  - form 태그 안에 placeholder 태그를 써서 입력창 안의 흐린 문구를 표시할 수 있다.
    - 타입을 text로 하면 텍스트 입력창만 출력된다.
      - submit을 넣으면 제출 버튼이 표시되며, value를 추가해 입력값을 바꿀 수 있다. button 태그는 입력값만 넣으면 된다.
      - required 속성을 form 태그 안에 넣으면, 필수 입력란으로 속성이 변경된다.

- input type 태그를 사용하여 선택하는 버튼을 만들 수 있다.
  - input type이 radio면 하나만 선택 가능하다. 단, 각각의 name 값을 동일하게 맞춰야 한다.
  - input type이 checkbox면 여러 개 선택 가능하다.
- value 값을 적어주어야 서버에 전송된 값이 어떤 값인지 구분할 수 있다. (eg. male, female)

<br>

<h2 id="#divide">배운 점 : div, span 태그</id></h2>
<p>
div는 block요소라 한 줄을 차지하고,<br>
span은 inline요소라 내용만큼만 차지한다.
</p>

```
<div>div는 사각형 박스로 영역이 잡힌다.<br>
줄이 바뀌면 다음 줄로 내려간다.</div>
<span>span은 문장 안의 일부처럼 표시된다.<br>
줄바꿈 없이 이어진다.</span>
```
