# 260909 TIL

## 배운 점

- html과 css의 차이에 대해 배웠다.
  - html : 구조물
  - css : 스타일

### CSS 연결 방식

- inline
  - 태그에 직접 &lt;style&gt; 속성으로

- internal
  - 같은 페이지에서 &lt;style&gt; 태그로

- external
  - HTML 밖 .css 파일을 &lt;link&gt; 태그로 연결

- 왜 분리하나?
  - HTML은 구조, CSS는 스타일. 나누면 스타일을 한 파일에서 고칠 시 연결된 여러 페이지가 한꺼번에 반영됨.

<br>

#### CSS 선택자

<hr>
<p>CSS에서 선택자를 이용하여 특정 태그들에만 속성을 부여할 수 있다.</p>

#### 전체 선택자

전체 선택자는 \*이다.

```css
* {
  color: blue;
}
```

#### 클래스 선택자

클래스 선택자는 .이다. 클래스 생성 시 .(생성명)으로 바로 생성할 수 있다.

```html
<div class="test">빨간색 글씨</div>
```

```css
.test {
  color: red;
}
```

#### 아이디 선택자

id는 페이지에 하나만 사용한다. (중복은 암묵적인 규칙 위반)
다중으로도 적용은 되나, 규칙상으로 유일하게 사용될 때에 쓰고 있다.

```html
<p id="only-one">아이디 선택자</p>
```

```css
#only-one {
  color: lightgreen;
}
```

#### 그룹 선택자

```html
<div class="group-a">group A</div>
<div class="group-b">group B</div>
```

```css
.group-a,
.group-b {
  color: teal;
}
```

#### 조합 선택자 - 자손 / 자식

```html
<div class="combo">
  <p>바로 안의 p</p>
  <div><p>더 깊은 p</p></div>
</div>
```

자손 선택자는 (공백)이고, 자식 손택자는 >이다.

```css
.combo p {
  color: teal;
}
.combo > p {
  font-weight: bold;
}
.combo > div > p {
  font-style: italic;
}
```

#### 형제 선택자

형제 선택자 : +는 바로 다음, ~은 뒤에 있는 모든 형제

```html
      <div id="sib">형제 1</div>
      <div class="sib">형제 2</div>
      <div class="sib">형제 3</div>
    </div>
```

```css
#sib + .sib {
  background: orange;
}

#sib ~ .sib {
  color: brown;
}
```

#### 속성 선택자

속성 선택자는 [속성명] 또는 (태그)+[속성="값"]이다.

```html
<input type="text" />
```

```css
[type] {
  background: mistyrose;
}
[type="text"] {
  background: mistyrose;
}
```

#### 상태 선택자

- :hover - 마우스를 올렸을 때에만
- :checked - 체크되었을 때에만
- :focus - 클릭되었을 때에만
- :disabled - 비활성화되었을 때에만

```html
    <button class="btn">hover</button>
    <label">
        <input type="checkbox">
        <span>checked</span>
    </label>
    <input type="text" placeholder="focus">
    <button disabled>disabled</button>
```

```css
.btn:hover {
  background: gold;
}
/* 체크박스가 선택되었을 때, 체크박스의 형제인 스판을 선택하겠다는 의미 */
[type="checkbox"]:checked + span {
  color: purple;
}
input:focus {
  background: lightblue;
}
button:disabled {
  background: silver;
}
```

#### 구조 선택자

구조 선택자는 :first-child, :last-child, :nth-child(n) 등이 있다.<br>
eg. [(태그) 리스트:차일드태그]

```html
<ul class="list">
  <li>item 1</li>
  <li>item 2</li>
  <li>item 3</li>
  <li>item 4</li>
</ul>
```

```css
.list li:first-child {
  background: yellow;
}
.list li:last-child {
  background: orange;
}
/* n 넣으면 n번째, odd 홀수, even 짝수 */
.list li:nth-child(even) {
  background: lightblue;
}
```

#### 가상요소 선택자

```html
<p class="badge">가상 요소</p>
```

```css
/* 가상요소 선택자는 ::이며 두 가지로 나뉜다 -> ::before 앞에 붙는 / ::after 뒤에 붙는 것.
   HTML의 컨텐츠에는 보여지지 말아야 하는, 없던 구조물이 CSS에서 작성되어 적용된다. */
.badge::before {
  content: "NEW";
  background: black;
  color: white;
}
.badge::after {
  content: "HOT";
  background: red;
  color: white;
}
```

---

### 태그의 우선순위

1. 같은 우선순위를 가지고 있을 시, 나중에 쓴 규칙이 이긴다.
2. 태그 < class < id < inline < !important 순으로 우선순위가 높다.

- 개발자 도구(F12 또는 우클릭 - 검사 탭)에서 해당 요소를 클릭하고, Styles를 보면 해당 우선순위가 몇 번째 줄에 적혀있는지 알 수 있다.

```html
<p id="strong" class="weak" style="color: green">
  2. 태그 < class < id < inline < !important 순으로 우선순위가 높다.
</p>
```

```css
/* 2번 규칙 예시 : 태그 < class < id < inline < !important */
#strong {
  color: orange;
}

.weak {
  color: hotpink;
}

/* 1번 규칙 예시 : 같은 우선순위일 경우 나중 것이 적용. */
p {
  /* !important는 가급적으로 사용 금지. */
  color: red !important;
}
p {
  color: blue;
}
```

<hr>

### 텍스트 스타일

#### font-family 글꼴

```html
<p class="ff-sans">기본(sans-serif) 계열 Font Test</p>
<p class="ff-serif">명조(serif) 계열 Font Test</p>
<p class="ff-web">구글 웹폰트 Font Test</p>

<h2>font-siz 크기 - 단위는 px, rem</h2>
<p class="fs-px">px : 고정 20px</p>
<p class="fs-rem">rem : 루트(16px) 기준 1.5배 = 24px</p>

<!-- em : 너무 계산이 복잡하여 사용하지 않는 단위. 
    <div style="font-size: 20px">
      <p class="fs-rem">rem : 루트(16px) 기준 1.5배 = 24px</p>
      <p style="font-size: 1.5em">em : 상대비율</p>
    </div> -->
```

- 구글 폰트에서 임베드 코드를 받아와서 헤드 부분에 넣으면 웹 폰트를 쓸 수 있다.
  - html과 CSS 두 곳에 다 임베드 코드를 작성해야 한다.
  - 이는 html에는 물자를 받아오고, CSS에서 어느 곳에 물자를 조달할지 정해 보낸다고 생각하면 이해가 쉽다.

```css
/* font-family 글꼴 : 앞에 있는 글꼴 사용, 없으면 다음 것 사용 */

.ff-sans {
  font-family: "돋움", sans-serif;
}
.ff-serif {
  font-family: "궁서", serif;
}
.ff-web {
  font-family: "Do Hyeon", sans-serif;
}

/* font-size : 데스크탑에서는 기본이 16px이다. */
.fs-px {
  font-size: 20px;
}
.fs-rem {
  /* 16px * 1.5 = 24px */
  font-size: 1.5rem;
}
```

#### font-weight 굵기

```html
<p class="fw-bold">굵게</p>
<p class="fw-normal">보통</p>
<p class="fw-thin">얇게</p>
```

```css
/* font-weight : 최소값은 100, 노말값은 400, 최대값은 900. */
.fw-bold {
  /* bold는 숫자로 700이랑 같다. */
  font-weight: bold;
}
.fw-thin {
  /* thin은 숫자로 100이랑 같다. */
  font-weight: thin;
}
```

#### line-height 줄간격

line-height : 기본값은 대략 1.15배 ~ 1.2배. 보통 1.5 ~ 1.8 정도를 사용.

```html
<p class="lh">
  줄이 여러 개일 때 위아래의 간격을 조정하는 태그.<br />
  숫자만 쓰면 글자 크기의 배수.<br />
  본문 가독성을 높이려고 할 때 제일 크게 체감된다.
</p>
```

```css
.lh {
  line-height: 1.8;
}
```

#### text-align 정렬

text-align : left(기본), center, right, justify(단어들 간의 간격을 균등히 배분)

```html
<p class="ta-center">가운데</p>
<p class="ta-right">오른쪽</p>
```

```css
.ta-center {
  text-align: center;
}
.ta-right {
  text-align: right;
}
```

#### color

```html
<p class="c-name">색상명</p>
<p class="c-hex">16진수</p>
<p class="c-rgb">RGB</p>
<p class="c-hsl">HSL</p>
```

```css
.c-name {
  /* 브라우저가 인식하는 기본 색상명(Web Safe Colors 216가지) */
  color: crimson;
}
.c-hex {
  /* RRGGBB - 범위 : 000000 (검정) ~ FFFFFF (흰색) */
  color: #ffaaaa;
}
.c-rgb {
  /* red(0~255), green(0~255), blue(0~255)
    rgba 입력 시 4번째에 투명도값인 alpha (0 ~ 1) */
  color: rgb(255, 215, 0);
  background: rgba(0, 0, 0, 0.5);
}
.c-hsl {
  /* 색상환표(0~360), 채도(0~100%), 명도(0~100%), (alpha 0~1)
  [숫자, 퍼센트, 퍼센트, (알파값)] */
  color: hsla(200, 50%, 50%, 0.5);
}
```

#### text-decoration

```html
<p class="td-underline">underline</p>
<a href="#" class="td-none">링크 밑줄 제거</a>
<p class="td-linethrough">linethrough</p>
```

```css
.td-underline {
  text-decoration: underline;
}

.td-none {
  text-decoration: none;
}

.td-linethrough {
  text-decoration: line-through;
}
```

#### letter-spacing / text-shadow

```html
<p class="ls">LETTER SPACIING</p>
<p class="shadow">TEXT SHADOW</p>
```

```css
/* letter-spacing 글자 사이 간격(자간) */
.ls {
  letter-spacing: 20px;
}

/* test-shadow 글자 그림자 */
.shadow {
  /* 그림자 위치 x축, 그림자의 위치 y축, blur(숫자) / 색상 */
  text-shadow: 5px 5px 5px red;
}
```
